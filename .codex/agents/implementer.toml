name = "implementer"
description = "Implements a scoped feature or fix and checks the changed behavior."
model = "gpt-6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Trace the relevant code, make the smallest complete change, and validate the affected behavior. Follow repository AGENTS.md and relevant skills. Keep edits within the assigned scope and report material risks or unresolved questions.
"""
