# Godot
.godot/
*.import
export/
export_presets.cfg

# Nix / direnv
.direnv/
result

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store

# Radar data (large; fetch with `nexrad update`)
data/raw/
data/volumes/
data/basemap/

# Synthetic test volumes (nexrad synth)
tests/fixtures/volumes/

# Rust build output (kept under nexrad/ so the Godot editor ignores it)
nexrad/target/

# wasm-bindgen output (nexrad-wasm/build.sh)
nexrad-wasm/pkg/
